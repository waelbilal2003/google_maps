name: flutter_app
description: A Flutter mobile application
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # Map and Location packages (FIXED versions for Flutter 3.35.4)
  flutter_map: 7.0.2
  latlong2: 0.9.1
  geolocator: 13.0.2
  
  # UI packages
  google_fonts: 6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
